# requirements_Group2.txt
# Project: University Recommendation System (Masaraq)
# Delivery for: ML_FinalProject_Group2
# Python: 3.9+
#
# Instructions:
# 1) Create and activate a virtual environment (Windows cmd.exe):
#      python -m venv venv
#      venv\Scripts\activate
#
#    On macOS / Linux:
#      python -m venv venv
#      source venv/bin/activate
#
# 2) Install dependencies:
#      pip install -r requirements_Group2.txt
#
# Notes:
# - The project uses an OCR path (easyocr) which may pull heavy dependencies (PyTorch).
#   If you prefer NOT to install PyTorch/easyocr, remove the `easyocr` and `google-genai`
#   lines below before running `pip install -r requirements_Group2.txt`.
# - pytesseract requires the Tesseract binary installed separately on the system.
#   Install Tesseract (e.g., on Windows download installer, on Ubuntu: `sudo apt install tesseract-ocr`),
#   and ensure the binary is on PATH or configure its path in config.py.
# - google-genai (Gemini) is optional: the OCR pipeline currently can call a GenAI step.
#   Do NOT store API keys in source; provide them via environment variables or config.py.
#
# Example alternate install (without easyocr & google-genai):
# 1) Edit this file to remove `easyocr` and `google-genai` lines.
# 2) pip install -r requirements_Group2.txt
#
# Packages (pinned versions used in development)
# Web application
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2
flask-babel==3.1.0

# Data processing and analysis
pandas==2.0.3
numpy==1.24.4
scikit-learn==1.3.0
matplotlib==3.7.2
joblib==1.3.2

# OCR and image processing
pytesseract==0.3.10
Pillow==10.0.0
opencv-python==4.8.0.76

# Arabic language support
PyArabic==0.6.15
langdetect==1.0.9

# Testing
pytest==7.4.0
pytest-flask==1.2.0

# Utilities
python-dotenv==1.0.0
easyocr==1.7.2
regex==2025.7.34
google-genai==1.31.0

# End of file
